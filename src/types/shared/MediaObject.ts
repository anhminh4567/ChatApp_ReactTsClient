export interface MediaObject {
  Name?: string; // The name of the media object
  MimeType?: string; // The MIME type of the media object (e.g., "image/jpeg", "video/mp4")
  FileUrl?: string; // The URL where the media object is stored
}
